

services:
  traceability:
    build: .
    image: riveting
    container_name: peppermove-riveting
    ports:
      - "5080:5080"
    restart: always
    env_file:
      - .env
    environment:
      - FLASK_ENV=production

networks:
  manufacturing_network:
    external: True